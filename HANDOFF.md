# 引き継ぎ手順（2026年9月）

体験デザインの検討で決まった内容を、いまのリポジトリに反映するための手順書です。

## ⚠ 最初に

**このパッケージは差分です。リポジトリを上書きしないでください。**

検討に使っていた作業コピーは38問時点のもので、いまの `bearko/MyCryptoEducation`（480問、UI基盤・レンジ回答・チャレンジ再設計・ショップ・称号カレンダーすべて実装済み）より大幅に古いです。丸ごと置き換えると442問と実装済み機能が消えます。

**同梱の2ファイルを追加し、既存ファイルには下記の編集を当ててください。**

| ファイル | 扱い |
|---|---|
| `docs/experience-design-framework.md` | **新規追加**（そのまま置く） |
| `src/answer-mode.js` | **新規追加**（そのまま置く） |
| その他 | 下記の手順に従って**既存ファイルを編集** |

---

## 手順

### 0. まず読む

`docs/experience-design-framework.md` を通読してください。**決定1〜4** が今回実装する内容です。以降の手順はすべてこの文書の仕様に従います。

### 1. 2ファイルを配置する

```
docs/experience-design-framework.md
src/answer-mode.js
```

`src/answer-mode.js` は現行スキーマ（`format: "choice" | "range"`、`choices` 無しの問題あり）に対応済みです。アプリと `validate.mjs` の両方から使います。**判定を2箇所に分けると必ずズレる**ので、必ずこのモジュール経由にしてください。

### 2. `CLAUDE.md` を更新する

4箇所です。

**(a) 原則3の見出しと本文**

`### 3. 不正解が罰にならない` を `### 3. 失敗は自分のものだが、罰されない` に変更し、末尾に追記：

> 同時に、**失敗をシステムのせいにさせないこと**も守ってください。プレイヤーに難易度のつまみ（難モードを続けるか4択に降りるか、ヒントを何段使うか）を渡し、その結果の失敗が自分の判断の結果になるようにします。この2つは矛盾しません。

**(b) 原則3-2を新設**（原則3の直後）

> ### 3-2. 報酬をプレイスタイルで差別化しない ★
>
> **答え方によって魔石・GUM・知識カードの量を変えてはいけません。** ゲーム内報酬をプレイスタイルで分けると「非効率的なプレイ」への忌避感が生まれ、学習ペースの違いが劣等感になります。
>
> 具体的に禁止するもの：
> - 難モードでの正解に報酬ボーナスを付ける
> - **「自力回答率」のような統計を表示する**
> - 特定の答え方を推奨する文言を出す
>
> 差をつけてよいのは、速さ・確信・記憶への残りやすさという**ゲーム外の報酬だけ**です。

**(c) 「次にやること」に追加**

`6. 問題DB拡張` の前に挿入：

```
6. 難モード（選択と裁量）— docs/experience-design-framework.md の決定2・決定4
   a. 消去法      … 万能フォールバック。既存データのまま全問に適用できる
   b. 数値入力    … 53問をカバー。テンキー、単位は固定表示
   c. 文字パネル  … 3×3／4×4 をなぞる。reading の付与が前提
   d. 無説明の初回起動 … 決定1
7. 問題DB拡張（以下、番号を繰り下げ）
```

**(d) 「判断に迷ったら」に導線**

> ゲームサイクルまわりは `docs/experience-design-framework.md`（体験デザインの枠組みと、決定1〜4の確定仕様）も併せて読んでください。

### 3. `scripts/validate.mjs` にチェックを足す

`src/answer-mode.js` から `answerMode` / `hintGroup` / `hintsFor` / `normalizeHints` / `answerText` を import して、次を追加します。

- **ヒントのグループ検査** — `choice` / `hidden` それぞれで使えるヒントが3本以上あるか。足りなければ**エラー**
- **`only` の値検査** — `"choice"` か `"hidden"` のみ。それ以外はエラー
- **hidden モードのヒント漏れ検査**
  - 正解そのもの（数値は単位を除いた形も）を含む → **エラー**
  - 誤答に言及している → **警告**（すべてが悪いわけではないため）
- **`reading` の書式検査** — 2〜12文字のかな・カナのみ
- **`panel` に振り分いたのに `reading` が無い** → エラー
- **集計表に難モードの内訳を追加**

文字パネルを実装したら、**経路生成を実際に走らせて100回以内に成功するかも検査**してください。通らない問題はCIで落とします。

### 4. ヒントを `{text, only}` 形式に対応させる

スキーマを後方互換で拡張します。**文字列のままのヒントは両グループで使う**ので、既存480問はそのまま動きます。

```json
"hints": [
  { "text": "基本構造は順次・分岐・反復の3つだよ。", "only": "choice" },
  { "text": "順に実行するもの、繰り返すもの、そしてもう1つだ。", "only": "hidden" },
  "「もし〜ならば」という形をとる構造だ。",
  "道が二手に分かれる様子を思い浮かべるといい。"
]
```

- `src/data.js` — 読み込み時に `normalizeHints()` で `{text, only}` に揃え、`answerMode()` の結果を `q.mode` に焼き込む
- `src/views.js` — ヒント表示を `hintsFor(q.hints, group)` 経由にする。`group` は「いま画面に選択肢が出ているか」で決まる（4択・消去法なら `"choice"`、文字パネル・数値入力・レンジなら `"hidden"`）
- `src/state.js` — セッションに難モードの状態を持たせる
- `scripts/build.mjs` — バンドルのモジュール順の**先頭**に `answer-mode.js` を足す（他が依存するため）

### 5. データを直す

`npm run validate` を走らせ、**エラー3件**を潰します。

| 問題 | 内容 |
|---|---|
| `sansu-018` | ヒントが答え「100」を含む |
| `sansu-020` | ヒントが答え「1000」を含む |
| `sansu-061` | ヒントが答え「12」を含む |

いずれも該当ヒントに `only: "choice"` を付け、**hidden 用のヒントを1本足して3本を確保**してください。

**警告16件**（誤答への言及）は1件ずつ判断します。判断基準は framework doc の「ヒントの出し分け」を参照。知識として成立するものは残してよい。

### 6. `reading` を付ける

`reading` を足せば文字パネルに回せる候補が **234問**あります。全部やる必要はありません。

- **`reading` が無い問題は自動的に消去法へ落ちる**ので、段階的に足していけます
- 用語・固有名詞に向いた教科（社会・国語・理科）から着手するのが効率的
- **述語（「減る」「すべて丸」）や説明句には付けない。** 用語だけ
- 英字の答え（`for` など）には付けない。かなとアルファベットのパネルは混在させられません

### 7. 実装する

**この順です。前の段が終わってから次へ。**

1. **消去法** — 4択を出し、正解を選ぶのではなく違うものを3つ潰す。誤って正解を消したら終了。既存データのまま全問に適用できるので最初に要る。**計算問題には割り当てない**（正解が出た時点で他が違うのは自明で、3回消す作業が冗長）
2. **数値入力** — テンキー、単位は固定表示（`[ 200 ] W`）
3. **文字パネル** — 3×3／4×4。8方向自己回避経路の生成、なぞり操作、連続タップでも成立させる。**1文字目のマークは出さない**
4. **無説明の初回起動** — ホームもチュートリアルも出さず小1の問題から。1問目は4択のみ、3問目あたりで難モードのUIが説明なしに現れる

各段で必ず `npm run validate` と `node scripts/smoke.mjs` を通してください。

### 8. 不正解時の3択を実装する

**自動降下は禁止です。** システムがつまみを勝手に回すと、失敗がプレイヤーのものでなくなります。

```
   ちがう。まだ続けてもいい。

   [ ヒントを見る ]  [ 4択に切り替える ]
```

- パネル・数値入力は**生きたまま**にする
- 「このまま答える」に専用ボタンは置かない（押しても何も起きないボタンは選択肢の重みを下げる）。文言で3択であることを示す
- 4択への切り替えはその問題では不可逆。次の問題では難モードに戻る
- 切り替え先は**素の4択**（消去法ではない。降りる方向であって横移動ではないため）
- **どの経路で正解しても報酬は同一**（原則3-2）

---

## 積み残し

- **パネルモードのヒントが読みの音に踏み込める問題。** 「4文字だ」「『に』で始まる」は知識ではなく探索の短縮なので原則禁止だが、いまの validate では検出できない。パネル実装時に「`reading` の一部がヒント本文に含まれていないか」の検査を足すこと
- **`applied`（応用編）は480問中38問にしかない。** 難モードを応用編にも適用するかは未決
- **チャレンジバトルの自由入力**は表記ゆれの受理集合生成が必要。難モードがパネル・数値入力になったことで前倒しの必要はなくなったが、削除はしないこと
