# CLAUDE.md

このリポジトリで作業するときに読むファイルです。

## これは何か

歴代の英雄（My Crypto Heroes のキャラクター）とともに、世界の義務教育カリキュラムを旅するクイズゲーム。静的サイトで、Vercel にデプロイします。

**目指している体験は次の5段階です。** 実装の判断に迷ったら、この流れを壊さないほうを選んでください。

1. 小学1年から駆け上がる（解ける爽快感）
2. 知識マップに空白が浮かぶ（忘れている領域の自覚）
3. 現行課程の新単元・海外の問題にぶつかる（翻弄）
4. 英雄がその場で教えてくれる（発見）
5. 世界の教室が少しずつ解けるようになる（成長）

---

## 絶対に守る設計原則

このプロジェクトは「ガチャ的な射幸心ではなく、知的好奇心で没入する」ことを狙っています。以下は何度も検討して決めた線です。**変更するときは必ず確認を取ってください。**

### 1. 強さの源は知識であって、素材ではない

チャレンジバトルの難易度ゲージを削るのは**知識カード**です。エクステンションは「どの知識が関連としてカウントされるか」を広げる触媒にすぎません。装備の量でゲージが削れる設計にすると、成長実感が知識から素材に移り、コンセプトが裏返ります。

バトル前に必ず内訳（どのカードが何ポイント効いたか）を表示してください。**削ったのは自分の知識であって装備ではない、と目に見えることが重要です。**

**ゲージの計算は「知識の総量」ではなく「関連分野の網羅率」で行います。** 総量は問題数に比例して
青天井に増えるため、DBを育てるほど飽和が早まります。実際、480問の時点で全英雄が100%に張り付き、
装備を何個つけても数字が動かなくなっていました。網羅率なら4800問でも壊れません。

```
基礎   = 直結カードの網羅率 × 0.6 ＋ 関連分野の網羅率 × 0.4
到達度 = min(1, 基礎 × 装備倍率 ÷ 難度係数)
難度係数   Common 0.4 / Uncommon 0.55 / Rare 0.7 / Epic 0.85 / Legendary 1.0
```

**この形なので、原則1は構造として保証されます。知識がゼロなら基礎が0で、装備を何個積んでも
0のままです。** 以前あった `gearCap`（装備は知識を超えないという後付けの上限）は要らなくなったので
撤去しました。装備は倍率としてしか効かず、合計で**最大2倍**が頭打ちです。**この頭打ちは
外さないでください。** 装備は英雄ごとに付けられて積み上がるので、外すと英雄が増えるほど
装備で到達度が上がる状態に戻ります。

HPは廃止しました。レアリティは体力ではなく「関連分野を何割埋めれば届くか」を表す難度係数です。
詳細は `docs/reward-economy.md`。

### 2. ランダム報酬を入れない（例外はクリスタルの抽選ひとつだけ）

ガチャ、日替わりランダムな品揃え、連続ボーナスは入れません。「良い物が出るまで待つ」構造を
作らないためです。GUM・知識カード・解説は解いた時点で確定です。

**唯一の例外が、クリスタルの抽選です。** 報酬が毎回きっちり同じだと周回が作業になるので、
「出会えたら幸運」な一撃を混ぜてあります（`engine.drawCrystal`）。
**射幸心の側に倒さないための縛りを3つ置いてあり、これは外さないでください。**

1. **どの族を貯めても、規定ポイントに届くまでの時間は変わりません。** 出にくい族は1回の
   当たりが大きく、出やすい族は小刻みに入ります。`当たる確率 × 当たりの期待ポイント` を
   族によらず `gumFor(q) × 上乗せ` に揃えてあるので、**特定の鉱物のために無理に周回する
   理由が生まれません。** smoke がこの等式を検査します
2. **外れても失うものはありません。** GUM も知識カードも解説も、正解した時点で確定です。
   画面にも「出なかった」とは書きません（外れが罰に見えるため）
3. **確率は図鑑の希少度そのものです。** こちらで盛ったり削ったりしていません。
   族の中では、ありふれた鉱物ほどよく出ます

レア寄りの族だけ、待たされるぶんの対価として期待値を**最大10%**上乗せしてあります
（`engine.RARE_BONUS`）。地道に集める道を否定しない幅として、ここは1割に留めてください。

実測（GUM=5相当の問題を10万問）:

| 教科 | 族 | 何問に1回 | 当たりの平均 | 50pt貯まるまで | 400pt貯まるまで |
|---|---|---|---|---|---|
| 理科 | 元素 | 4問 | 20pt | 10問 | 79問 |
| 社会 | 貴金属 | 6問 | 30pt | 10問 | 81問 |
| 情報 | 鉱石 | 6問 | 29pt | 10問 | 80問 |
| 算数・数学 | 石英 | 19問 | 99pt | 10問 | 77問 |
| 国語 | 生物起源 | 23問 | 123pt | 9問 | 74問 |
| 外国語 | 宝石 | 43問 | 236pt | 9問 | 72問 |

### 3. 失敗は自分のものだが、罰されない

- 不正解でも知識カードと英雄の解説が手に入ります
- 不正解時の文言は「面白い単元に当たった」。**「間違えました」とは書きません**
- 解説スキップ設定が OFF でも、**不正解の解説は必ず表示します**
  （スワイプだけは、テンポのために出す場所をセッションの終わりへ移しています。
  **出さないのではありません。** 外した問題は開いた状態で並びます）
- チャレンジに負けても、知識カードが増えれば次回は最初からゲージが削れています

同時に、**失敗をシステムのせいにさせないこと**も守ってください。プレイヤーに難易度のつまみ（難モードを続けるか4択に降りるか、ヒントを何段使うか）を渡し、その結果の失敗が自分の判断の結果になるようにします。この2つは矛盾しません。

### 3-2. 報酬をプレイスタイルで差別化しない ★

**答え方によって GUM・クリスタル・知識カードの量を変えてはいけません。** ゲーム内報酬をプレイスタイルで分けると「非効率的なプレイ」への忌避感が生まれ、学習ペースの違いが劣等感になります。

具体的に禁止するもの：

- 難モードでの正解に、**GUM・クリスタル・知識カードの**ボーナスを付ける
- **「自力回答率」のような統計を表示する**
- 特定の答え方を推奨する文言を出す

差をつけてよいのは、速さ・確信・記憶への残りやすさという**ゲーム外の報酬だけ**です。

### 点数だけは例外です

**点（`S.score`）は、答え方や踏みこみ方で変わってかまいません。** 消去法で削った数
（＋2／＋3／＋5）も、ヒントを使った本数（`10 − 本数×2`）も、応用編（＋15）も点を動かします。

**この線引きの根拠は「残高になるかどうか」です。** 3-2 が防ぎたいのは、持ち物の差が
そのまま学習ペースの差として積み上がることです。点は**買えるものが何も無く**、
称号の条件にも入っていません。画面に出るだけで、次の一手を有利にしません。
だから深く踏みこんだ人と、そうしない人のあいだに差が残りません。

**点を何かに使えるようにするなら、この例外は成立しなくなります。** 買い物にも称号にも
つなげないでください。つなげるなら、削った数による加点のほうを先に外してください。

### 4. カレンダーは記録装置であって報酬装置ではない

連続ログイン日数のボーナスもペナルティも入れません。動機が「知りたいから」から「途切れさせたくないから」に移るためです。

### 5. ヒントは答えの直前で止める

第3ヒントでも答えそのものは書きません。処理の手順は渡しても、最後の一手は残します。

- ✗ 「10 + 3 だ。」
- ✓ 「10を作ったあと、5から使った分を引く。それを10に足す。」

`npm run validate` がこれを検出します。警告が出たら書き直してください。

### 6. 通貨の役割を混ぜない

- **クリスタル** — 知識の副産物。解いた教科から確定で落ちる。**買えない**。鉱物の豆知識を持つ
- **GUM** — 行動量の副産物。問題数に応じて貯まる。クリスタルを買える

GUM でクリスタルの「知識の副産物」のほうを買えるようにすると、苦手分野に手を出す動機が消えます。

**魔石は廃止しました。** 6教科が4魔石に潰され、さらに6族へ展開される写像のねじれが
複雑さの正体でした。加えて魔石は中身のないトークンで、クリスタルには鉱物の知識が入っています。
**中身のないほうを消しました。** `data/gemstones.json` も `S.gems` もありません。
`npm run validate` が、レシピに `cost`（旧・魔石）が残っていたらエラーにします。

GUMの額は学年の梯子そのものです（`engine.gumFor`）。小1が1、小6が6、中3が9、
世界の問題が10。**ヒントを使っても減らしません。**ヒントは罰の対象ではないためです。
応用編にも上乗せしません。1問の上限が10GUMなので、難易度は学年だけで決めます。

### 7. 報酬系に有能感を担わせない

メタ分析の結論として、ゲーミフィケーションは内発的動機・自律性・関係性を高めますが、
**有能感への影響は最小**です。「できるようになった感じ」は問題を解くことからしか来ません。

報酬系が狙うのは次の2つだけです。

- **自律性** — 何を作るか、誰に装備させるか、次に誰に挑むか
- **関係性** — 英雄。装備を持たせることがその英雄との関係を深める行為になる

また、報酬の種類を増やしても外的動機は時間とともに減衰します（新奇性効果）。
増やすのは種類ではなく、**報酬が開く新しい問い**のほうです。

根拠は `docs/research-edtech.md`。

---

## コマンド

```bash
npm start          # http://localhost:3000
npm run validate   # 問題DBの整合性チェック（在庫一覧も出る）
npm run pick:commons  # 写真の候補を見て選ぶ画面（手動・要ネットワーク）
node scripts/fetch-mch-assets.mjs   # MCHの英雄・背景・エネミー・マインちゃん（手動）
npm run build      # dist/index.html （単一ファイル版）
npm run shots      # 全画面を撮って docs/screens/ に並べる（手動・要 playwright）
node scripts/smoke.mjs   # 全画面を実際にクリックして回る通しテスト
```

**コードを変えたら `npm run validate` と `node scripts/smoke.mjs` を両方通してください。** smoke は jsdom が要ります（`npm install --no-save jsdom`）。

CI（`.github/workflows/ci.yml`）は PR ごとに validate → build → smoke を走らせます。

---

## 構成

```
data/          問題と設定。ここを増やすのが本体
  questions/   教科別JSON（kokugo / sansu / rika / shakai / gaikokugo / joho）
  heroes.json  英雄・チャレンジ問題
  advice.json  ホームでマイちゃんが話す文言と、その出しわけ条件
  titles.json  称号と、その取得条件
  crystals.json クリスタル100種。和名・英名・族・希少度・Amount・豆知識。価格は持たない。
               希少度は図鑑の Amount ÷ 全種の合計 × 100（小数第3位で丸め）。
               `families`（6族）と `subjectToFamily`（教科↔族の1対1）もここ
  curriculum.json 教科ごとに、どの学年帯にその教科が存在するかの表
  images.json  コモンズの写真の台帳。search と alt だけ手で書き、残りは取得時に埋まる
  extensions-curated.json エクステンション108種。MCHの実在品から、現実の歴史・神話・
               文学・科学に由来が参照できるものを抜き出した台帳。**書くのは事実だけ**
               （名前・系統・ランク・またがる教科・由来）。ゲージ寄与もクリスタルの要求も
               知識カードの条件も `data.js` の `buildExtensions` が導く
  figures.json 図版（SVG）
public/        画像
  heroes/      ロスターと Rep.画像（id + 10000）。原本は 64x64
  backgrounds/ ホームのボタン背景。small/ は単一ファイル版に畳む縮小コピー
  extensions/ / icons/ / materials/crystals/
  commons/     Wikimedia Commons の写真（640px）。small/ は単一ファイル版に畳む縮小コピー
src/
  normalize.js 表記ゆれの吸収（LLMは使わない）
  engine.js    出題ロジック・難易度ゲージ。★DOMに触れない純粋関数だけ
  data.js      JSONの読み込みと索引づくり
  state.js     ゲーム状態。進行ぶんを localStorage に保存する
  views.js     画面描画
scripts/       validate / build / smoke / アセット取得
docs/          実装計画と設計判断の記録・体験デザインの枠組み・報酬系の確定仕様
               review-points.md には、**人が見ないと確かめられないもの**を集めてある
               （画面・由来の断定度・勝手に決めた判断・数字の釣り合い）
```

**`src/engine.js` に DOM 操作を持ち込まないでください。** 出題や難易度の計算をここだけで追えることが、テストのしやすさを支えています。

**英雄の画像を大きく出すときは `image-rendering: pixelated` を付けてください。** 原本が 64x64 しかないため、既定の補間だとただの低解像度画像に見えます。ドットを残すと意図した表現に見えます。

**クリスタルの価格を手で書かないでください。** 図鑑の希少度から `engine.crystalPrice` が算出します（`max(5, round(50 / 希少度%))`）。`data/crystals.json` に価格の欄はありません。

**クラフトが払うのは族ポイントで、鉱物そのものは減りません。** 鉱物は手に入れた時点で
ポイントに変わり（`S.points`）、図鑑（`S.crystals`）には残り続けます。鉱物を消費させると、
たまに出た高い鉱物が安いレシピに丸ごと食われますし、クラフトのたびに図鑑が欠けて
豆知識が読めなくなります（「手に入れると読める」と決めてあるため）。

**クラフトの釣り合いを手で書かないでください。** `data/extensions-curated.json` に書くのは
由来が参照できる事実だけです。ゲージ寄与・クリスタルの要求・知識カードの条件は、
ランクとまたがる教科から `data.js` の `buildExtensions` が導きます。品を足すときに
数字を釣り合わせる手間が要らず、釣り合いが崩れることもありません。**validate も同じ関数を
呼びます**（2箇所に分けると必ずズレるため）。

| ランク | 条件 | 件数 | ゲージ寄与 | クリスタル | 知識カード |
|---|---|---|---|---|---|
| 初伝 | 由来が1教科 | 61 | +15 | 50pt | その教科で5枚 |
| 中伝 | 由来が2教科 | 43 | +30 | 150pt | 2教科それぞれ8枚 |
| 奥伝 | 由来が3教科以上 | 4 | +60 | 400pt | 3教科それぞれ12枚 |

クリスタルはまたがる教科ぶんの族へ均等に割ります。**その品の分野に対応しない族は
要求できません**（`npm run validate` がエラーにします）。

**知識カードは、またがる教科それぞれで要ります。素材だけでは作れません。**
ここがクイズ → 素材 → クラフト の回路に知識を通す関門です。大唐西域記（奥伝）を作るには、
社会・国語・外国語のどれも実際に解いていないと届きません。**この条件を外さないでください。**
外すと「素材の量＝強さ」に戻ります。

**奥伝には、さらに由来カードが要ります**（`originCard`）。その品の元になった人物や
出来事の問題を解くと手に入る、ただ1枚のカードです。教科の広さだけでは届かず、
**その品そのものを知っていること**が要ります。クラフト画面の「由来をたずねる」から
直接その問いに挑めます——**運でその1問に当たるのを待たせないため**です。
`npm run validate` が、由来カードを配る問題が無い／2問ある／その問題自身が閉じている、
のいずれもエラーにします。

**ランクに MCH のレアリティを使わないでください。** 上位レアリティだけを見ていると、
算数・数学がゼロのままになります。コンパスとアンティーク地球儀は MCH 内では下位ですが、
由来は幾何学の作図と地球周長測定という一級の題材です。`npm run validate` が、
ランクと教科数が食い違っていたらエラーにします。

**族は教科に結び付いています。** `data/crystals.json` の `subjectToFamily` が
その表で、**1対1です**（社会↔貴金属・外国語↔宝石・理科↔元素・情報↔鉱石・
国語↔生物起源・算数・数学↔石英）。どの教科を解いてきたかがクラフトに効くので、
作りたい品が、解く教科を選ぶ理由になります。

**それでも、どの鉱物を選んでも損得は出ません。** 価格が 50 ÷ 希少度 なので
`希少度 × 価格` は常に 50 になり、クラフトのポイントも**払った GUM そのもの**
（`engine.crystalPoints`）です。**族の中でも族の間でも、1 GUM あたりの重みは変わりません。**
以前ここに「族を教科に結び付けるな」と書いてあったのは、族ごとに価値が偏ることを
恐れたためですが、要求をポイントで測るかぎり偏りは生まれません
（1個ぶんの目安は `engine.CRYSTAL_UNIT` = 50pt）。

**族を空にしないでください。** その族を要求するレシピが永久に満たせなくなります。
`npm run validate` が、鉱物が1つも無い族と、教科と1対1になっていない対応表をエラーにします。

**族の大きさは揃っていません。それでかまいません。**

| 教科 | 族 | 種類 | 最安 | 平均 | 全種そろえる |
|---|---|---|---|---|---|
| 理科 | 元素 | 14 | 8pt | 29pt | 400pt |
| 社会 | 貴金属 | 5 | 8pt | 133pt | 666pt |
| 情報 | 鉱石 | 22 | 13pt | 287pt | 6,321pt |
| 外国語 | 宝石 | 42 | 19pt | 982pt | 41,240pt |
| 算数・数学 | 石英 | 14 | 51pt | 106pt | 1,480pt |
| 国語 | 生物起源 | 3 | 102pt | 125pt | 376pt |

現実の鉱物がそう分布しているだけで、揃えようとすると分類のほうが嘘になります。
**空の族が無くなった時点で、機能としては足りています。**

この凸凹が**効かないところ**：クラフトの進み方。要求も持ち高も同じポイントの物差しで
測るので、どの族を貯めても 1 GUM あたりの重みは変わりません。

この凸凹が**効くところ**：図鑑の埋まり方。元素は8ptから小刻みに埋まり、宝石は
大物を待つことになります。**これは有利不利ではなく族の性格です。** そう読めるように
画面に出してください。

ひとつだけ気をつけるところ。**称号「石を読む者」は5種集めると開きますが、生物起源は
3種しかありません。** 国語だけを解いていても届かないので、ショップ（GUMはどの族でも買える）
がその逃げ道になっています。族の種類数を減らすときは、ここを確認してください。

**クリスタルを知識カードにしないでください。** 知識カードは難易度ゲージを削る唯一の手段です。クリスタルは GUM で買えるので、入手で知識カードが付くと GUM でゲージが買えることになり、原則1が裏返ります。鉱物の話は「手に入れると読める」ところまでにしてあります。経緯は `docs/design-decisions.md`。

**レンジ回答で、答える前に点数の見込みを出さないでください。** 点は `1000 × (1 − 幅 / 許容幅)` なので、幅と点が分かると許容幅が割れます。許容幅は正解年から決まるので、そこから年が逆算できます。画面に出すのは自分で決めた幅だけです。

**文字パネルの `reading` は用語にだけ付けてください。** 述語（「減る」「すべて丸」）や
説明句には付けません。英字の答えにも付けません。かなとアルファベットのパネルは混ぜられません。
`reading` が無い問題は自動的に消去法へ落ちるので、**段階的に足していけます。**

**問題に `reading` を1つ足しただけでも `npm run touch-check` を走らせてください。**
盤面は問題IDから決まるので、**読みと ID の組み合わせによっては、指のぶれ3pxで
なぞりきれない盤面ができます**（実際に2問続けて作りました）。長さの問題ではなく
組み合わせの問題なので、`npm run validate` では捕まりません。落ちたら `reading` を
外して消去法へ回してください。

**文字パネルを触ったら `npm run touch-check` を走らせてください。** jsdom には座標も
暗黙のポインタキャプチャも無いので、通しテストではタッチの不具合を捕まえられません。
実物のブラウザでタッチのポインタを流し、全問をなぞって確かめます（playwright が要ります）。
実際にこの検査で、**137/160問で途中のマスが混ざっていた**ことが分かりました。

**消去法では、選択肢の文字を押せるようにしないでください。** できるのは ✕ で消すこと
だけです。「誤っているものを消す」と「正しいものを選ぶ」が同じ画面に同居すると、
**どちらをすればよいのか分からなくなります。** 押せる場所は行ごとに ✕ ひとつに絞り、
文字は読むためだけのものにしてあります。

| 方式 | 帯 | 文言 |
|---|---|---|
| 4択 | 青 | 正しいものを 1つ 選ぶ |
| 消去法 | 朱 | 誤っているものを 3つ 消す（＋いま何点か／次はいくつか） |

**帯には「次に消せたらいくつ入るか」を出してください。** 削るかどうかを決めるための数字が
見えていないと、駆け引きになりません。

**消すごとに増やしてよいのは点だけです。** GUM・クリスタル・知識カードは動かしません
（原則3-2）。点はヒントの本数でも増減する数字で、買えるものが何もないので、
プレイスタイルの差が持ち物の差になりません。**ここを材料の側に広げるときは確認を取ってください。**

**「4択に切り替える」のボタンは、消去法にも置いてあります。** 文字が押せない以上、
正しいものを1つ選びたい人の逃げ道はここだけです。**降りるかどうかは常にプレイヤーの手に
あり、システムは勝手に降ろしません**（決定2）。降りてもその問題かぎりで、次の問題では
また難モードから始まります。難易度のつまみは2段になりました——**何個まで削るか**と、
**そもそも削るのをやめて4択へ降りるか**です。

**スワイプは、束としてならふつうのセッションにも出ます。** 混ぜてはいけないのは
「1問だけ」で、**8問続けて出るぶんにはテンポが切れません**（むしろそれが持ち味です）。
出題が束でできるようになったので、在庫ごと切り離す必要は無くなりました。
**スワイプだけの10問**という入口は別に残してあります（`kind: "swipe"`）。
`npm run validate` は、マス目の在庫からスワイプを除いて数えます。

**スワイプでは、選択肢の文字を押す以外に「はらう」で答えられます。** カードを
正しいと思うほうへ払う、左右のボタンを押す、矢印キーを使う——どれも同じです。
**踏み切る前（56px 未満）で放せば元に戻ります。** 途中まで動かすとその側が光るので、
確定する前にどちらを選んでいるかが見えます。

**スワイプの写真は、題名から答えが割れないものにしてください。** コモンズの題名は
被写体の名前そのものです（"Dmitri Mendeleev 1890s"）。**PD と CC0 は表示義務が無いので、
出題中だけ題名を伏せています**（作者・ライセンス・出典は出したままなので、条件より厚い）。
**CC BY は題名も表示の条件なので伏せられません。** CC BY の写真を使うときは、
題名を読んでも答えにならない問いにしてください。`npm run validate` が、CC BY の写真を
使っているスワイプ問題を警告します。

**写真が問いにどう効いているかは、3通りあります。** 35問を数えると A 6問・B 28問・C 1問でした。

| | 中身 | 例 |
|---|---|---|
| **A 読む** | 写真を見ること自体が答えの根拠 | 「この虫のあしは何本?」「文字盤の Ⅸ が表す数は?」 |
| **B 見分ける** | 写真は「この人」「この機械」の指す先 | 「この人が唱えた考えは?」 |
| **C 飾り** | 写真が無くても問いが成立する | ← **これは作らないでください** |

**C を作らないでください。** 一度ありました（`sansu-105`「1から100までの和を速く出すには?」に
ガウスの切手）。問題文がどこも写真を指していないと、**絵として置いてあるだけ**になります。
「この〜」で写真を指すか、写真を読ませるか、どちらかにしてください。

**B は残します。** 顔が分からない人には2択の当てずっぽうになりますが、外しても知識カードと
解説が入り（原則3）、ふりかえりで顔と事実が結びつきます。**そこが受け皿です。**

**画中に名前が刷りこまれていても、答えにならなければ構いません。** ガウスの切手には
`C.F.GAUSS` と入っていますが、問うているのは正十七角形と足し方なので漏れません。
**むしろ「この人」が誰か分かるぶん、B の問いが成立します。**

### 二段構え — 知識を問うてから、誰かを問う ★

**`applied`（応用編）を「発見者は誰か」に使えます。** 正解したときだけ出る問いなので、
**1問の中で「定理そのもの」と「一歩踏みこんだ発見者の把握」を順に問えます。**

```
本題   この人が少年のころ使ったとされる、「1から100までの和を速く出す」足し方は?
        → 両はしから組にして足す
応用編 この人の名前は?           ← 正解したときだけ出る。外したらスキップ
        → ガウス
```

**この順番が大事です。** B型（「この人が唱えた考えは?」）は、**顔が分からないと
最初の一歩も踏み出せません。** 順番を逆にすると、本題は知識だけで答えられ、
**そこを通った人にだけ写真が意味を持ちます。** 写真が「答えるために見るもの」に
変わるので、B型の弱点がそのまま解けます。`sansu-105` がその形です。

報酬は応用編の決まりどおりで、**＋15点と抽選がもう1回**。GUM も知識カードも
増えません（原則6・原則3-2）。

**応用編が「この人の名前は?」を問うなら、問題に `hideTitle: true` を付けてください。**
クレジットの題名が被写体の名前そのものなので（"DBP 1955 204 Carl Friedrich Gauß"）、
**付けないと出題中に答えが出ています。** 伏せられるのは表示義務の無い PD・CC0 だけで、
`npm run validate` がそれ以外に付けたらエラーにします。作者・ライセンス・出典は
伏せません（外すとライセンス条件を満たさなくなる）。**解説の中では題名も出します。**
そこまで来れば答えは済んでいるためです。

**画中に名前が刷りこまれた写真は、この形では使えません。** ガウスの切手には
`C.F.GAUSS` と大きく入っているので、題名を伏せても写真そのものが答えを配ります。
**本題だけなら構いません**（`sansu-104` は正十七角形を問うので漏れない）。

**スワイプには置けません。** 途中で何も挟まないのがあの形式の芯です
（`npm run validate` がエラーにします）。二段構えにしたい問題は、
4択・消去法・文字パネル・数値入力のどれかにしてください。

**スワイプの `alt` は、問題ごとに書いてください。** 台帳（`data/images.json`）の alt は
被写体を名指ししているので（「周期表を作ったメンデレーエフの肖像写真」）、そのまま出すと
2択が消えます。`npm run validate` が、alt が選択肢をそのまま含んでいたらエラーにします。

**スワイプにヒント・Tips・応用編・`needs` は置けません。** 途中で何も挟まないのが
この形式の芯で、出す場所が無いものを必須にすると、埋めるためだけの文が増えます。
`npm run validate` がエラーにします。

**報酬はほかの方式とまったく同じです。** GUM も知識カードもクリスタルの抽選も、
4択で答えたときと1つも変わりません（原則3-2）。**2択は当てやすいので減らしたくなりますが、
減らした瞬間に「非効率なプレイ」ができます。** 変えてよいのは出す情報の量だけです。

### 1セッションは、出題形式の「束」3つでできています ★

**以前と方針が逆になりました。** かつては `spreadModes` で同じ形式が3問以上続かない
ように並べ替えていました。いまは**同じ形式を続けて出します**（`engine.planRun`）。
操作のしかたを覚え直す回数が減るぶん、問題そのものに集中できます。
**飽きは「ばらけさせる」ではなく「束を3つに分ける」ほうで防ぎます。**
`spreadModes` は撤去しました。復活させないでください。

```
文字パネル 3問 → スワイプ 8問 → 消去法 4問     （合計15問）
```

**束の長さは操作の手数で決めます**（`engine.MODE_WORK` と `BLOCK_WORK`）。
問題数をそろえると、重い形式の束だけが長く感じます。

| 形式 | 手数 | 1束 |
|---|---|---|
| スワイプ | 1.0 | 8問 |
| 4択 | 1.4 | 6問 |
| 消去法 | 2.0 | 4問 |
| レンジ | 2.4 | 3問 |
| 数値入力 | 2.4 | 3問 |
| 文字パネル | 2.8 | 3問 |

**1セッションの長さは 9〜18問（中央値14）で、毎回変わります。** `RUN_LENGTH` は
「1マスに何問あれば1セッション成立するか」の目安として残してありますが、
**セッションの長さではありません。** 画面に「10問」と書かないでください。

**形式を選ぶのは完全な乱数です。** 正答率や履歴で選ぶと、得意な形式ばかり来る人と
苦手な形式ばかり来る人に分かれます。**ここに「その人の傾向」を持ち込まないでください。**

**3問そろわない形式は束にしません。** 1〜2問では「まとめて出す」意味がないためです。
在庫が薄くて束が3つ組めないときは、組めただけで出します。

**最後の1問が「いまの範囲の外」であることは変えていません。** 越境問題を持つ形式の束を
最後に回し、その束の末尾に置いています。

`node scripts/smoke.mjs` が、束が3つあること・同じ形式が離れて2度出ないこと・
束の長さが手数どおりであること・形式の選ばれ方が偏らないことを、80セッション回して見ます。

### 出題形式ごとの段（Lv.1〜Lv.3）★

**学年は「どの教育課程の問題か」、段は「同じ学年の中でどれだけ踏みこむか」です。**
別の軸なので、混ぜないでください。中学1年のままでも、用語を言い当てるだけの問いと、
似たものを見分ける問いと、理由や例外を問う問いでは手ごたえが違います。

問題に `level`（1〜3）を書きます。目安はこの3つです。

| 段 | 中身 | 例 |
|---|---|---|
| Lv.1 | 用語と定義が1対1。基本の計算。見出しの語をそのまま問う | 「『美しい』の品詞は?」「8 + 5 = ?」 |
| Lv.2 | 似たものを見分ける。条件がひとつ付く。公式を当てはめる | 「『書く』の活用の種類は?」「800円の2割引は?」 |
| Lv.3 | 複数の知識を結ぶ。理由・例外・境界を問う。逆向きに問う | 「『べし』に含まれない意味は?」「相似比2:3のときの面積比は?」 |

**段は形式ごとに別で持ちます**（`state.modeLevel`）。消去法は得意だが文字パネルは苦手、
という差はふつうにあるので、ひとつの数字にまとめると必ずどちらかが合わなくなります。

**上がるだけで、下がりません**（`engine.levelUps`）。その形式の束で**4分の3以上**
とれた回に1つだけ上がり、Lv.3で止まります。束が3問に満たない回は上がりません。
記録からの再挑戦（`noReward`）でも動きません。
**下げる仕組みを入れないでください。** 外した回が「降格」として画面に出ることになり、
原則3（失敗は罰されない）と正面からぶつかります。

**段は報酬を変えません**（原則3-2）。GUM は学年だけで決まります（原則6）。

**白いマスの優先を、段より先に効かせてください**（`engine.rankPool`）。
順番を逆にすると、段が上がった人が白いマスにたどり着けなくなり、知識マップが埋まりません。
いまは 白いマス → 未出題 → 既出 の**それぞれの中で**、いまの段のものを先に出しています。

**在庫は 形式 × 段 で見ます。** 出題は形式ごとの束で組むので、そこが空だとその段に
上がった人の束が組めません。`npm run validate` が表を出し、**Lv.1 が空ならエラー**、
上の段が空なら警告（梯子がまだ短いだけで、出題は一段下りて成立します）。

**問題を足すときは、そのマスに段の差をつけてください。** 同じマスが Lv.1 ばかりだと、
「同じ学年の中の梯子」になりません。`npm run validate` が、段が1つしか無いマスを警告します。

### 図形は線で描き、イメージは写真を採ってください ★

**この2つは別のものとして扱います。**

| | どこに | 何で |
|---|---|---|
| **図形** — 三角形・円・回路図・柱状グラフ・箱ひげ図 | `data/figures.json` | SVG を手で書く |
| **イメージ** — もの・生きもの・人物・風景 | `data/images.json` | `fetch-commons.mjs` で採る |

理由は2つです。

1. **線だけで描くと、別のものとして読まれます。** たまごの輪郭だけを描くと
   「楕円」に見えます。実際に、パンを丸屋根の塊として描いたら、丘ともトンネルとも
   読めるものになりました。**写真ならその曖昧さが出ません**
2. **写真のほうが情報量が多く、見ていて飽きません。** 同じ絵柄の線画が並ぶより、
   目が動きます

図形のほうは逆で、**線で描くほうが正確です。**「底辺6cm・高さ4cm」の三角形は
写真にできませんし、余計なものが写りこみません。

**いらすとやは使えません。** 商用利用は1つの制作物につき20点までで、
それを超えると有償です。加えて規約が「素材を主役にした商品」を禁じており、
**絵を見て答える形式は素材が主役にあたります。** `data/images.json` の
`allow`（PD・CC0・CC BY）に乗るものだけを使ってください。

**選択肢の語が見た目で浮くときは、絵にしてください**（`choiceArt`）。

「かたかなで書く言葉はどれ?」を語のまま並べると、**カタカナの語だけが見た目で浮いて、
読まずに当てられます**（`kokugo-012`）。語のかわりに絵を出すと、その語がかなで書くものか
外来語かは、見ただけでは分かりません。

```
問題に   "choiceArt": ["obj-yama","obj-pan","obj-tamago","obj-inu"]
        ↑ data/images.json の写真でも、data/figures.json の図版でもよい
```

**絵の選択肢に使える写真は PD・CC0 だけです。スワイプと同じ線です。**
コモンズの題名は被写体の名前そのもので（"No-Knead Bread"・"Labrador Retriever snow"）、
**どの絵が何のことかを言い当てるのがこの形式の中身**です。題名が出た時点で問いが消えます。
表示義務が無いのは PD と CC0 だけなので、その2つしか題名を伏せられません。
`npm run validate` が、CC BY の写真を `choiceArt` に置いたらエラーにします。

**ここは一度ゆるめて、戻しました。** 以前「題名は要るものにだけ付ける」
（`views.needsTitle`・CC BY 4.0 は題名が不要）としていたのは、**題名を長さの問題として
見ていたから**です。実際は漏れの問題で、スワイプを PD・CC0 に絞ったのと同じ理由でした。
`needsTitle` は撤去しました。

**クレジットは選択肢のまとまりの下に1行で出します。** 題名は出しません——出さなくてよい
写真しか使わないからです。作者の名前が、その写真の出典ページへのリンクになります。

**台帳の行には `"titleFree": true` と書いてください。** 「この写真は被写体の名前を伏せた
まま出す」という印です。`scripts/fetch-commons.mjs` がこの印を見て CC BY を採らず、
`--list` も PD・CC0 の候補を `◎` で分けて出します。**採ってから使えないと分かるのが
いちばん時間を食います**（実際に食いました）。`npm run validate` も、印の付いた行に
CC BY が入っていたらエラーにします。

**日用品の写真は、検索語だけでは当たりません。** 「dog sitting portrait」で投稿者名
`DogTwo` に当たり、カフェにいる人の写真が来ました。「chicken egg」ではマヨネーズの皿が
来ました。**`node scripts/fetch-commons.mjs --list <キー>` で候補を並べ、
`commons` に `File:〜` を書いて採ってください。** 人物や出来事と違い、
日用品は題名が被写体を表していないことが多いためです。

**◯ の数はその語の文字数です**（パン＝◯◯、たまご＝◯◯◯）。
**どの絵が何のことかを言い当てるための手がかりで、答えの手がかりではありません。**
`npm run validate` が、**正解の文字数だけ他と違う**問題をエラーにします
（絵を見なくても当てられるため）。

**答えたあとは、4つとも語を出します。** 何だったのか分からないまま終わらせないためです。

**絵の `aria-label` に名前を書かないでください。** 「山の絵」と書いた時点で、
そこが答えになります。いまは「絵の選択肢」までにとどめてあります。
**そのぶん、この形式は目で見ることが前提になります**（スワイプの写真と同じです）。

`npm run validate` が、選択肢と数が合わない `choiceArt`、figures.json に無い図版、
選択肢が見えない方式（文字パネル・数値入力・レンジ）に回る問題への `choiceArt` を
エラーにします。

**答えを1手で決めさせないでください。** 数値入力と文字パネルは、決定ボタンを押すまで
確定しません。1手目で決まると、押す意味を取りちがえた人がそこで終わってしまいます。
**消去法とスワイプだけは1手で決まります。** どちらも「決定を遅らせる」よりも
「踏みこむかどうかを自分で決める」ほうを取りました。消去法は押せる場所が ✕ ひとつしかなく、
スワイプははらう向きがそのまま答えなので、**押す意味を取りちがえようがありません**
（スワイプは途中で放せば戻せるので、確定の前に一段あります）。
**押す場所によって意味が変わる形にはしないでください。**

**ヒントに読みの音を書かないでください。** 「4文字だ」「『に』で始まる」は知識ではなく
探索の短縮です。`npm run validate` が、読みの先頭3文字以上を含むヒントと、文字数を明かす
ヒントを警告します。

**報酬で増やすのは、種類ではなく問いです。** 問題に `needs: "<カード名>"` を書くと、
そのカードを持つまで出題されません（`engine.questionOpen`）。外的報酬は露出が続くと
動機を下げますが、増やすのが**次の問い**なら頭打ちになりません
（`docs/research-edtech.md` §5）。解いた瞬間に「◯問が開きました」と出します。
`npm run validate` が、鍵になるカードを配る問題の不在と、`needs` の輪をエラーにします。

**初回起動に、学年をひとつ聞く画面があります**（S-31・`views.vGrade`）。
**決定1（無説明の初回起動）をひとつだけゆるめた画面です。** 聞くのは
「何年生から始める?」ひとつ、答えるのは1タップで、**説明も遊び方も置きません。**
大人が小1から駆け上がるのがこのゲームの入口ですが、**そこを選べないと
「自分の話ではない」と感じる人がいます**（自律性・原則7）。
**ここに説明や案内を足さないでください。** 足した時点で、ゆるめた1画面が
チュートリアルになります。

**選んだ学年は「梯子の下端」です**（`S.startGrade` → `engine.subjectGrade`）。
進行そのものは知識マップのままで、**ここが決めるのは下端だけです** ——
数値を2つ持つと必ず食い違います。その教科がもっと上からしか無いときは、
梯子ぜんぶを見ます（情報は中学からなので、小3から始めても中1が出ます）。

**初回起動に説明を足さないでください。** 初めて開いた人には、ホームもチュートリアルも
出さずに小学1年の問題を出します（`views.startIntro`・決定1）。1〜2問目は4択だけにして
基本形を見せ、3問目から難モードを黙って出します。マリオ1-1がまず「右に進める」を
教えるのと同じ組み立てです。**「教わらずにできた」という体験から始まることが、
教育を題材にしたこのゲームのテーマそのものです。** チュートリアルも吹き出しの案内も
置かないでください。smoke が、初回にホームが出ないことと、説明らしい文言が無いことを
検査します。

**初回起動の画面には、問題と解答しか置きません**（S-01）。次のものは出しません。

| 出さないもの | なぜ |
|---|---|
| 国・学年・教科・進み具合（`1 / 5`） | 先に数字で言うと、解く前に身構える |
| 学年チップ・単元・段（`Lv.1`） | **難しくなっていくことは、解いていれば分かります** |
| 英雄の行とヒントボタン | ヒントは「教わる」ほうの道具です |
| 4択の帯（`正しいものを 1つ 選ぶ`） | 4つ並んでいれば、押せることは見れば分かります |

**消去法の帯だけは残します。** ✕ が何をするものかは、そこにしか書いていません。
**3問目でここに帯が初めて現れることが、難モードが来た合図そのものになります。**

**画面の上は空けたままにしてあります**（`.introtop`）。あとで演出が入る場所なので、
**いま別のものを置かないでください。** 入れるときに動かすことになります。

**短い選択肢（6文字以下）は2×2に並べます。** 縦に4つ積むと、読む前に目が上から下へ
流れます。碁盤に置くと4つが一度に目に入ります。

**穴のあいた文は `stem` に書いてください。** 問題文の上に大きく出て、`prompt` は
「○に入るのは?」のような短い問いかけになります。

```json
"stem":   "ぼく○がっこうへいく。",
"prompt": "○に入るのは?"
```

`npm run validate` の同一問題文の検査は、**`stem` と `prompt` を合わせて見ます**
（`stem` を使うほど `prompt` が短く似てくるので、`prompt` だけで見ると別の問題が
「同じ問題文」に見えます）。穴うめの型は他に41問あるので、順次移せます。

**正解をいつも同じ位置に置かないでください。** 書いていると、自然に「正解を先に書いて、
あとから誤答を足す」形になります。実際、**4択482問のうち92.7%が先頭**になっていました。
読まずに当てられる状態です。並べ替えは問題IDから決まる形で一度かけてあるので、
**足すときは位置を散らしてください。** `npm run validate` と smoke が、全体で35%、
教科ごとで45%を超えて偏ったらエラーにします。数の並び（「43/45/47/49」など）は
昇順のままでかまいません。そちらは値が位置を決めるので、当てる手がかりになりません。

**英語の -er / -or / -ar 由来のカタカナ語は、語尾に長音符を付けてください。**
コンピューター・サーバー・センサー・ブラウザー・プリンター・ルーター・パラメーター など。
2019年の JIS Z 8301 改正で、原則つけると改められました。それ以前は3音以上の語で省く
とされていたので、古い表記が混ざります。**混ざっていること自体が読み手の負担**なので、
DBの中では揃えます。`npm run validate` と smoke が、抜けているものをエラーにします。

省いた形も誤りではありません。**答えになる語は `accept` で受け、なぜ両方正しいかを
`note` に書いてください。** `accept` と `note` の中は検査の対象外です——短い形をわざと
引き合いに出す欄だからです。

**メモリー（-ory）やソフトウェア（-ware）は別の決まりなので、この一覧に入れないでください。**
分野によっては短い形が定着しているものもあります（量子コンピュータ、パラメータ推定）。
**そういう語は、表記を揃えたうえで `note` にその旨を書きます**（`joho-039`）。

**どちらの書き方でも正しいときは、両方受けて理由を注釈で書いてください。**
`accept` に別表記を並べると、文字パネルでどちらをなぞっても正解になります
（例 `joho-022`：サーバー／サーバ）。**`accept` を置くなら `note` が必須です**
（`npm run validate` がエラーにします）。黙って通すと、片方が誤りだと思ったまま終わります。

**`note` は、解説を省く設定でも必ず出ます。** 表記のゆれのように、知らないと次に
ぶつかったとき迷うものを置く欄です。テンポ優先モードで正解したとき、`note` があれば
トーストではなく**注釈だけの小さい判定**を出します（解説そのものは出しません）。

**問題データの `format` は `choice` か `range` です。** 省略すると `choice` 扱いです。`range` は `year`（紀元前は負の数）を持ち、`choices` と `answer` は持ちません。`precision`（既定1.0）で許容幅を締めたり緩めたりできます。

**チャレンジの `ch.qs[].v` は難しい順に並べてください。** 段階0（ゲージが削れていない）が `v[0]`、段階3（削りきった）が `v[3]` です。知識を積むほど問い方がやさしくなります。逆に並べると、知識を積むほど難しくなります。

**挑戦で配る知識カードは、その英雄の `rel.cards` からだけです**（`engine.challengeCard`）。プールが有限なので、挑み続けても無限には増えません。ここを広げると、知識を積まずに挑戦を繰り返すだけでゲージが埋まるようになり、原則1が崩れます。

**写真はクレジットとセットでしか出しません。** CC BY は作者・ライセンス・出典の表示が条件なので、
台帳（`data/images.json`）にその欄が欠けている画像は `photoHTML` が何も出しません。`npm run validate`
も、欄の欠けた写真を参照している問題をエラーにします。**クレジットの表示を外さないでください。**
外した瞬間にライセンス条件を満たさなくなります。

**写真に使えるのは PD・CC0・CC BY だけです。** 許すライセンスは `data/images.json` の `allow` に
書いてあり、`scripts/fetch-commons.mjs` がそこに無いものを弾きます。**CC BY-SA は入れないでください。**
改変物に同じライセンスが波及します。NC（非営利限定）と ND（改変禁止）も入れません。

**ヒントに置く写真は、それだけで答えが割れないものに限ります。** 置き場所は問題データの
`imageAt`（`prompt` / `hint` / `lesson`）で決めます。`hint` は第3ヒントに添えるので、原則5と
同じ線が要ります。**いちばん効くのは `lesson` です。** 不正解でも解説は必ず出るので（原則3）、
外した人ほど写真に出会います。

**写真は `small/` のほうを単一ファイル版に畳みます。** base64 は元の1.33倍になるので、640px のまま
畳むと配布ファイルが実用外の大きさになります。背景画像と同じ扱いです。

**残バッテリーは Battery Status API です。iOS Safari は未対応なので出ません。** 取れたときだけ表示する作りにしてあります。実機のiPhoneで出ないのは仕様です。

### 出題を選ぶ（S-04 / S-25）— 文字で説明しない ★

作り直しの柱は3つです。**どれも「増やさない」方向の決まりです。**

1. **文字で説明しない。** 段の上がり方も束の組み方も、遊んでいれば分かります
2. **選択肢を増やさない。** 6教科ぜんぶ並べると、選ぶだけで疲れます
3. **スクロールさせない。** 1画面に収めます（`.sel` が 100dvh の3層）

**教科は毎回くじで3つだけ出します**（`engine.drawSubjects`）。在庫のある教科から
引きます。**正答率や履歴は使いません**——形式のくじと同じ理由で、得意不得意で
偏らせないためです。**ホームへ戻って入り直しても引き直しません。** 引き直せると、
欲しい教科が出るまで往復することになります。出発した時点で捨てて、次に引き直します。

**範囲（小学校/中学校/世界）の選択は外しました。学年は教科ごとに決まります**
（`engine.subjectGrade`）——その教科でまだ埋まっていない一番下の学年です。
**知識マップを埋めることが、そのまま学年を上げることになります。** 専用の数値を
持たせないのは、進行が2つになると必ず食い違うためです。梯子の一番上が「世界」なので、
**海外の問題はその教科を中3まで埋めた人のところへ自然に来ます。**

**科目名は、その漢字を習う学年になるまで ひらがな で出します**（`engine.subjectLabel`）。
実際に効くのは小1のある国語と算数だけで、理科・社会・外国語は小3から、情報は
中学からなので最初から漢字です。**算数は中学から「数学」に変わります**（課程どおり）。

**予告した形式のまま出発させてください。** 教科を選んだ時点で `planRun` を1度だけ
呼び、その結果（`S.select.run`）をそのまま `startRun({ built })` へ渡します。
描き直すたびに引き直すと、予告と中身が食い違います。

**エネミーは形式ごとに1体つきます**（`engine.enemyOf`）。番号の並びが教科に
対応しています（310国語 / 320算数・数学 / 330理科 / 340社会 / 350外国語 / 360情報）。
**同じセッションのあいだは化けません**——描き直すたびに別の敵になると、倒した
手ごたえが消えます。`npm run validate` が、並びの重なりと絵の欠けを見ます。

**札の右側は「知識カード」の枚数です。** 解いた数だけ確実に入る唯一のもので、
難易度ゲージを削るのもこれです（原則1・原則3）。GUM は行動量の副産物、
クリスタルは抽選なので、**「倒したら必ずこれ」とは書けません。**

**教科ごとの絵は `data/subjects.json` にあります**（英雄・背景・エネミーの並び）。
**そこに入るのは絵の話だけです**——どの学年を出すかは知識マップから導くので、
持たせません。取り込みは `node scripts/fetch-mch-assets.mjs`（手動）。

**外したものが2つあります。**

- **スワイプだけの入口。** 形式は毎回くじで3つ選ばれるので、スワイプもその中に
  来ます。`kind: "swipe"` で引く仕組みは engine 側に残してあります
- **解説スキップの設定。** マイページの「設定」タブへ移しました。
  遊ぶたびに触るものではないので、置き場所はそちらが合っています

### 出題の画面は4つの層です（S-05〜S-10 / S-26）★

**主役は設問と解答です。** バトルはその上に細く畳みます——大きく取ると、
問題を読む前に戦況を読むことになります。

| 層 | 中身 |
|---|---|
| **バトル演出** | 英雄と敵、体力の帯、Q.1／あと3問、3つの束の帯。背景は教科ごと |
| **設問** | 問題文（`stem` があればその上に大きく）、写真とクレジット、図版 |
| **補足** | 教科・単元・段・ヒント・⚙。**ヒントは小さく畳みます**（原則5の道具なので） |
| **解答** | 4択・消去法・文字パネル・数値入力・レンジ。キーボードの出る形式は下に余白 |

**初回起動だけ、この層立てに乗せません**（決定1）。バトルも立てません。

**1つの束が1つの wave です。** 3束なので1セッションで3回たたかいます。
いま何束目の何問目かは `S.run.plan` と `S.run.i` から毎回数えます——別に持つと、
記録からの再挑戦や4択への降り方でズレます。

**正解するとこちらが殴り、外すと向こうが殴ります。** 演出は MCH の
`01_single_damage.png`（900×900 を 9×9 に割ったコマ送り）です。

**2体は向かい合わせます。** MCH の絵は英雄も敵も**左を向いている**ので、
そのまま並べると2体とも同じほうを見ます。左に立つ**英雄のほうを左右反転**
させてください（`.bt-side.hero .bt-ch`）。敵を反転させると外を向きます。
**跳ねる演出と倒れる姿勢にも反転が要ります**——`transform` は最後に書いた
ものだけが効くので、コマごとに `scaleX(-1)` を書き直しています。

**ヒントと設定はポップアップで出します**（S-27 / S-28）。**元の画面を動かさない
ためです。** ヒントを流れの中に置いていたころは、開いた瞬間に解答エリアが下へ
押し出されて、答えるのにスクロールが要りました。**解答エリアを動かすものを、
設問の途中に差しこまないでください。**

**ヒントの本数が増えるのは、ポップアップの中の「もう一段」を押したときだけです。**
入口のヒントボタンは、1本も見ていないときだけ1本目を開き、2回目からは読み直す
だけです。点はヒントの本数で決まるので（`10 − 本数×2`）、**読み返しただけで
減らないようにしてあります。** 使い切ってもボタンは押せるままです——一度見た
ヒントに戻れなくなるためです。`npm run touch-check` が、開いても解答エリアが
動かないこと・開き直しで本数が増えないことを見ています。

### 束の切れ目に、接敵と戦果の画面を挟みます（S-29 / S-30）★

**敵に出会った → 問題を解いて倒す → 倒せた／逃げられた**、という1本の筋を
見えるようにするための2枚です。出題の画面ではバトルを上に小さく畳んでいるので
（主役は設問と解答）、**戦いの手ざわりを受け持つ場所がどこにも無いままでした。**

| | 中身 |
|---|---|
| **S-29 接敵** | 形式の名前と全◯問。英雄が画面の外から入ってきて、敵の体力が0→100%に満ちる。GO! で問題へ |
| **S-30 戦果** | 撃破！／逃げられた……。その束ぶんの 解けた・応用も突破・正答率と、手に入れたクリスタルと GUM。NEXT で次の束かリザルトへ |

**束があるときだけ挟みます**（`views.gated`）。ID を名指しで渡す道——記録からの
再挑戦・クラフトの「由来をたずねる」・検査——は1問だけのことが多く、そこに
wave の演出を挟むと、**聞きたい1問にたどり着くのが遠くなります。**
初回起動にも出しません（バトルそのものを立てないため・決定1）。

**成績は、束の始めと終わりの差で出します**（`S.run.mark`）。別に数えると、
4択への降り方や再挑戦でセッション全体とズレます。

**敵・英雄・背景は、束の終わりに控えます**（`S.run.waves[]`）。そのあとの
`battleStep` が次の敵を立ててしまうので、戦果の画面から見にいくと**もう別の敵**に
なっています。背景も、最後の束では次の設問がもう無いので、引きにいくと既定の絵に
変わります。**控えたものから描いてください。**

**英雄と敵は 128px（原本の2倍）で出します。** ここは2体を見せるための画面
なので、出題の画面の 56px とは別です。

**逃げられたときだけ、マイちゃんが手の打ち方を言います**（ヒーローの強化と、
応用問題による追加攻撃）。**罰ではなく道順です**（原則3）。倒しきったときは
出しません——うまくいった人に助言は要りません。

**「たおした！」の一瞬の表示（`.bt-flash`）は撤去しました。** 戦果の画面が
同じことを言うので、2回言うことになります。

**釣り合いは「その束の8割に正解したら敵が倒れる」です**（`engine.ATTACK_RATE`）。
敵の体力は**残りの問題数**から決めるので、1体目を早く倒して2体目が湧いても、
**最後の1問で満タンの敵が出てくることはありません。** 英雄が倒れるのは
セッション全体の6割を外したあたり（`FALL_RATE`）。

**英雄が倒れても wave は続きます。** 正解したぶんの GUM・知識カード・
クリスタル・解説は、倒れているかどうかで**1つも変わりません**（原則3）。
変わるのは**敵を削れなくなること**だけで、そのぶん倒したときの上乗せが
取れなくなります。**ここは確認済みの仕様です。**

**倒した数と倒しきった束の数だけ、GUM と点が上乗せされます**
（`engine.battleBonus`／たおした敵1体 +3GUM +20点、倒しきった束1つ +5GUM +30点）。
**原則3-2（報酬をプレイスタイルで差別化しない）をここで一段ゆるめています。**
答え方ではなく**正解の数**で動く上乗せなので、難モードで答えた人と4択で答えた人の
あいだに差は出ませんが、**GUM が問題数以外の要素で動くのは初めてです。**
経緯は `docs/review-points.md` §21。

**ヒーローの強化やパーティ編成はまだありません。** 「英雄を強くする楽しさ」を
「勉強する動機」につなげるための土台として、いまは体力と攻撃力を MCH の
実パラメーターから読んでいるだけです（`data/battle-stats.json`）。

**答えたあとの解説は、描き直しても残ります。** 判定は `#verdict` へ命令的に
差しこむので、`render()` が走ると消えていました（⚙ を押しただけで解説が飛びます）。
`S.run.verdict` に描いたものを控えて、`replayVerdict` が同じものを描き直します。
**`#verdict` に何かを足すときは、控える側にも足してください。**

### 必殺技は、応用編への2つ目の入口です（S-32 / S-33）★

**解説を省く設定にしていると、応用編にたどり着く道がありませんでした。**
応用編のボタンは解説の中にしか無いので、テンポ優先モードの人はいつまでも
挑めません。そこを開けるのが英雄のパッシブスキルです。

```
正解 → 「必殺技発動チャンス!」（S-32）→ 応用問題 → 正解 → カットイン（S-33）→ 敵に大ダメージ
```

**サイコロは振りません**（`engine.skillChance`）。隙を見せるかどうかは
**問題IDと英雄から決まります**。運で出るようにすると、**出るまで引き直す
遊び方**ができてしまい、原則2（ランダム報酬を入れない）と正面からぶつかります。
割合としては `SKILL_RATE`（45%）ですが、**1問ごとに見れば決まっています。**

**解説を出す設定のときは出しません。** そちらには応用編のボタンが元からあるので、
入口が2つ並ぶことになります。**`applied` を持たない問題でも出しません**
（挑む先がありません）。初回起動でも出しません（バトルを立てないため）。

**必殺技が動かすのは敵の体力だけです**（`SKILL_POWER` = ふつうの3倍）。
GUM も知識カードも増えません（原則3-2・原則6）。応用編そのものの報酬
（＋15点と抽選がもう1回）は、これまでどおりです。

**カットインは MCH の `Style/Cutins/passive_skill_cutin.css` を写したものです**
——斜めの帯、色ドッジの光、2倍のドット絵、四方に縁を付けた技名。
**技の名前は `passive.name.ja` そのもの**で、こちらで付けていません
（`data/battle-stats.json` の `skill`）。

**カットインを `app` の下に置かないでください。** 最中に `render()` が走ると
`app.innerHTML` ごと消えます。`document.body` に置いてあります
（`position:fixed` なので見た目は変わりません）。

**`position:fixed` のものは、全面撮りだと写りません。** つなぎ合わせるときに
描き直されないためです。画面に重なるものは `viewport: true` を付けて、
表示領域のぶんだけ撮ってください（`scripts/screens.mjs`）。

### 画面には番号が振ってあります（S-01〜S-33）★

**レビューで「どの画面の話か」を一意に指すためです。** 一覧と最新の絵は
`docs/screens/index.html`、`npm run shots` で撮り直せます。

**単位はビューではなく状態です。** クイズは1つのビューですが、4択・消去法・
文字パネル・数値入力・レンジ・スワイプで見た目が別物なので、「クイズ画面が変」と
言われても直す先が決まりません。だから S-05〜S-10 に分けてあります。
解説（S-11）と応用編（S-12）も、同じビューですが別の番号です。

**ポップアップにも番号があります**（S-27 ヒント・S-28 設定・S-32 必殺技チャンス・
S-33 カットイン）。下に何の画面が
あっても見た目はポップアップのものになるので、状態としては別物です。

**S-11 はバトルの無い解説です。** ふつうのセッションには必ず敵がいるので、
答えたあとは S-26 になります。バトルを立てないのは初回起動だけなので、
**S-11 が撮れるのはそこだけです。**

**番号は使い回さないでください。** 画面を足すときは `scripts/screens.mjs` の
末尾に足します。途中に差しこむと、過去のレビューの番号が別の画面を指すように
なります。`npm run validate` が、抜けと重なりをエラーにします。

**判定は `views.screenId()` が持ち、一覧は `scripts/screens.mjs` が持ちます。**
2つに分かれていますが、**`npm run shots` が撮るたびに突き合わせて、ズレたら落ちます**
（ズレたまま古い絵が残ると、指摘先が別の画面になります）。smoke も形式ごとの
番号を見ています。

**設問には元から ID があります**（`sansu-105`）。出題の画面を撮ると、その設問IDも
一覧に出ます。**実機で見ているときは、URLの末尾に `#review` を付けてください。**
画面の隅に `S-06 ・ gaikokugo-071` のように出ます。ふだんの画面には出しません
（遊ぶ人に要らないものだからです）。

**縦に長い画面は上から4000pxで切っています**（ショップ12633px・クラフト13316px）。
webp の上限を超えて書けないためで、切ったことは一覧に出ます。

**クラス名の衝突に注意してください。** 2度やっています。

- ホームの日付チップが `.cal`（`position:absolute`）なので、カレンダーの表は `.calgrid`
  にしてあります。同じ名前にすると表が絶対配置になって画面外に飛びます
- 消去法の帯は `class="band cut"` なので、✕ ボタンは `.xcut` にしてあります。
  `.cut` と付けたら `width:46px` が帯にも当たり、**帯が幅46pxの縦長の塊**になりました
- ふりかえりの行が `.rv` なので、その中の写真は `.photo.rvpic` にしてあります。
  `.rv` と付けたら「10問のふりかえり」を数える検査が20を数えました
- スワイプは `.swp-` でそろえています。**`.sw-` は使えません。** 知識マップの凡例
  （`.sw-ok` / `.sw-st`）と設定のスイッチ（`.sw`）が先に使っています
- 形式の束の帯は `.plan` / `.pseg` です。`.panel`（枠）や `.place`（ヘッダの左）と
  別物なので、省略形にしないでください

**`.band` は `cut` と `pick` を修飾子として持ちます。** 新しいクラス名を足すときは、
この2語とぶつからないか確かめてください。

**見た目の崩れは `npm run touch-check` でしか捕まりません。** jsdom には寸法が無いので、
通しテストは何も言わずに通ります。実物の Chromium で、帯の幅と高さ、✕ ボタンの大きさと
位置、選択肢が縦に重なっていないか、画面の外へはみ出していないかを見ています。
**スワイプの「はらう」も、ここでしか確かめられません**（座標が要るので jsdom では
押すところまでしか試せません）。実際にカードをドラッグして、途中で放せば戻ること、
踏み切ればその向きで確定することを見ています。**レイアウトに触ったら走らせてください。**

**CSSのカスタムプロパティに `url()` を入れないでください。** Chrome は `var()` を使った側のスタイルシート（`src/styles.css`）からの相対パスとして解決するため、`./public/...` が `./src/public/...` になって404します。画像は要素の `style` 属性に直接書いてください。

---

## 作業のときの注意

### 問題を追加する

`data/questions/<教科>.json` に追記します。書式は README を参照。追加後は必ず `npm run validate` を通してください。

**在庫は 1マスあたり10問が最低ラインです。** 1セッションが10問なので、下回るマスは出題が短く終わります。**実在48マスすべてが10問に乗りました**（計492問）。`npm run validate` が下回ったマスと教科を警告します。

**`needs` を持つ8問は、この表には入っていますが最初は出てきません。** 鍵になるカードを
手に入れるまで在庫から外れます。表の数字と、画面に出る在庫がずれるのはそのためです。

なお「解放済みの範囲だけ」で見ると、まだ1セッションに満たない範囲があります。第3・4章は英雄の解放で開くので、序盤の在庫が薄いのは設計どおりです。

**カリキュラムに無いマスに問題を置かないでください。** 理科・社会の小1小2は生活科に統合されていて教科として存在せず、外国語は小3から、情報は中学校からです。`data/curriculum.json` がその表で、`npm run validate` が表に無い組み合わせをエラーにします。実在するのは 60マスのうち **48マス**です。表に無いマスに問題を作ると、どの教育課程にも対応しない作り物になります。

**在庫は「教科×学年」のマス単位で見てください。** `npm run validate` が 教科×学年 の表を出し、空きマスと10問未満のマスを警告します（「−」はカリキュラムに無い組み合わせ）。教科の合計が足りていても、特定の学年に偏っていれば、その学年を選んだ人の体験は薄いままです。**現在はどのマスも10問以上です。** どの教科のどの学年を選んでも、1セッション（10問）が成立します。

### アセット取得スクリプトは手動実行

`scripts/fetch-*.mjs` は CI で走らせません。相手先のサーバに負荷をかけないためです。

**手元での手順は `docs/photo-workflow.md` にまとめてあります**（取り込み・確認・push、
うまくいかないときの直し方まで）。開発コンテナからは Wikimedia に出られないので、
取り込みは人が走らせることになります。

**探すのは Claude、運ぶのは人です。** `commons.wikimedia.org` は組織のポリシーで
**403** になります（proxy のログに `connect_rejected` が残ります）。`WebFetch` も同じです。
**`WebSearch` は通る**ので、**File 名を決めるところまでは Claude ができます。**
台帳の `commons` に File 名を書いておけば、手元での作業は
`node scripts/fetch-commons.mjs` の1回だけになります——探す手間も、選ぶ画面も要りません。

**ライセンスの取り違えは機械が止めます。** `allow` に無いものは採りませんし、
`titleFree: true` の行に CC BY が来たら validate がエラーにします。
**止まらないのは「写っているものが違う」だけ**で、そこが人の目の出番です
（婚礼の写真・顔の接写・カフェにいる人・マヨネーズの皿が実際に来ました）。

**自分で選びたいときは `npm run pick:commons` を使います。** 候補が写真のまま並ぶ画面が
`http://127.0.0.1:3100/` に出るので、よさそうな1枚を押します。押した時点で webp が書かれ、
題名・作者・ライセンス・出典も台帳に埋まります。`node scripts/fetch-commons.mjs` は、
`commons` や `entity` がもう書いてあってまとめて採るだけのときに使います。
候補を集める部分は `scripts/commons-lib.mjs` にまとめてあり、**両方が同じ関数を呼びます**
（2箇所に分けると必ずズレるため）。

**カテゴリは「深く」たどります。** `deepcategory:` で下位カテゴリまで見ます。
**大きなカテゴリは、中身がほとんど下位カテゴリだからです** ——`Category:Ships` の
直下にファイルはほぼ無く、直下だけを見ていた頃は21行が「候補がありません」で
全滅しました。**転送カテゴリも空を返します**（`Category:Equus caballus` →
`Category:Horses`）。

**ライセンスは、探すときに絞ります。** コモンズの写真の多くは CC BY-SA で、
こちらは使えません。**集めてから落とすと、候補が数件しかない行では必ず全滅します。**
構造化データ（`P6216=Q19652` パブリックドメイン / `P275=Q6938433` CC0）で
先に引いて、そのあとライセンス無指定の段も足します（構造化データの付いていない
写真を落とさないため）。**`titleFree` の行では無指定の段を足しません** ——
PD・CC0 しか使えないので、足すと画面が × で埋まります。

**音声も動画も PDF も「ファイル」です。** `gcmtype: "file"` は名前空間6のことなので、
雲のカテゴリから解説音声（`.ogg`）が、鶏のカテゴリから `.tif` が来ました。
`filetype:bitmap` と拡張子で写真だけに絞っています。

**うまくいかないときは、源ごとの件数が出ます**（`源ごとの件数: カテゴリ（深）12 / 全文検索 0`）。
どの源が空だったのかが分かるので、直す先が決まります。

**探す手がかりは5種類あり、この順に見ます。**

| 欄 | 書くもの | どこから引くか |
|---|---|---|
| `commons` | `File:〜.jpg` | その1枚を名指し |
| `entity` | 日本語の語（`"食パン"`） | ウィキデータの項目 → P18「画像」 |
| `wikipedia` | 記事名（`"イヌ"` / `"en:Bread"`） | その記事の先頭に出ている画像 |
| `category` | コモンズのカテゴリ名 | **下位カテゴリまでたどった中のファイル** |
| `search` | 英語の検索語 | 全文検索（**最後の保険**） |

**まず `entity` を書いてください。** ウィキデータの P18 と記事の代表画像は、
**人が「これがその物の画像だ」と決めたもの**です。全文検索はファイル解説ページの全文が
対象なので、投稿者名やカテゴリの文言にも当たります。`dog sitting portrait` は投稿者名
`DogTwo` に当たって**カフェにいる人の写真**を、`chicken egg white` は**マヨネーズの皿**を、
`Shakespeare Chandos portrait` は**ヘンデルの肖像画**を連れてきました。
**「Xの写真がほしい」に対して、全文検索はそもそも道具が違いました。**

**それでも、採れた画像が問題文の意図と合っているかは人が見て確かめてください。**
ライセンスは機械が保証できますが、写っているものが適切かは保証できません。
画面で選ぶ形にしたのは、その確認を**選ぶ前**に移すためです。

**候補がゼロのときは、絞りこみのかけすぎです。** `insource` と長い検索語を重ねると、
条件を全部満たすファイルが無くなります。`entity` を書けばたいてい解決します。

手元での手順は `docs/photo-workflow.md` に、詳しい経緯は同じファイルにあります。

### エクステンションの由来を文章として出すとき

`data/extensions-curated.json` の `origin` には、**史実と伝説が混ざっています。**
知識カードや解説として画面に出す前に、諸説あるものが「と伝わる」「という逸話がある」で
書かれているか、1件ずつ確かめてください。**ゲーム内で断定すると、このゲームがいちばん
やってはいけないことになります。**

- すでにぼかしてあるもの … へし切長谷部（「圧し切ったという逸話から」）など34件
- 要確認 … ブケファロス（少年期の逸話はプルタルコスの伝える話）、三日月宗近（名の由来は通説）、
  フランベルジェ（語源に諸説）など。**『三国志演義』『西遊記』のように出典の作品名が
  書いてあるものは、作品についての記述なのでそのままで構いません**

108種のうち、どの教科にまたがるかで独自ランクが決まります（初伝1教科61 / 中伝2教科43 /
奥伝3教科以上4）。**MCHのレアリティは使いません。** 上位レアリティだけを見ると算数・数学が
ゼロになるためです。経緯は `docs/reward-economy.md` §4。

### ライセンス

| 素材 | 条件 |
|---|---|
| MyCryptoHeroes（英雄・エクステンション・背景） | MCH社から許諾取得済み |
| クリスタル100種 | CC0。**ただし商標にはCC0が及ばないため、提供元の名称をゲーム内で使わない**。個々の鉱物名（Gold、Beryl など）は一般名詞なので可 |
| Wikimedia Commons の写真 | **PD・CC0・CC BY のみ**（`data/images.json` の `allow`）。**CC BY-SA・NC・ND は使わない**。作者・ライセンス・出典を画像のすぐ下に必ず表示 |

---

## 現在地

- **834問**（4章構成・実在48マスすべてが10問以上・小学校の30マスはどこも17問以上）、英雄12体、SVG図版58点。
  うち**8問は `needs` で閉じていて、鍵になるカードを手に入れると開きます**。
  **34問はスワイプ専用**で、ふつうのセッションには出てきません（在庫が別）。
  小学校 501問 / 中学校 254問 / 世界 79問
- **コモンズの写真82点**（台帳は103行・採れていない行が21）— 取り込みは
  `scripts/fetch-commons.mjs`（手動）。PD・CC0・CC BY のみで、クレジットは必ず画像の下に
  出る。置き場所は問題データの `imageAt`。**採れた写真は、問題に結び付けてはじめて
  「使った」ことになります。** 台帳に採れたまま使われない行をためないでください
- 4択・**レンジ回答（年代当て）**・応用編・3段階ヒント・別の英雄によるTips
- **何年生から始める?**（S-31）— 初回に1度だけ、学年をひとつ聞く。選んだ学年が
  **梯子の下端**になる（`S.startGrade`）。進行そのものは知識マップのまま。
  **決定1をひとつだけゆるめた画面で、説明は置かない**
- **無説明の初回起動** — 学年を選ぶと、ホームもチュートリアルも出さずに**いきなり
  その学年の問題**が5問。1〜2問目は4択だけで、3問目から難モードがすっと現れる。
  **説明はしない。** 解き終わって初めてホームが出る。2回目からはホームから始まる
- **難モード** — 問題は毎問そこから始まる。**4択へ降りるのは常にプレイヤーの手**で、
  システムは勝手に降ろさない。降りてもその問題かぎり。**どの経路で正解しても報酬は同じ**
  （原則3-2）。内訳は消去法209・文字パネル185・数値入力53・4択34・スワイプ16・レンジ11。
  **1セッションは形式の束3つ**で、同じ形式は続けて出る（`engine.planRun`）
  - **消去法** — **何個まで削るかを、プレイヤーが決める。** ✕ を押すと1つ消えて、
    その場で当たり外れが決まる。正しく消せれば点が入り、**1つ目＋2、2つ目＋3、3つ目＋5**と
    増える（`answer-mode.CUT_SCORE`）。確信があるほど深く削れて点が伸びる。
    まちがえて正解を消したらそこで終わりだが、**消せたぶんの点はそのまま残る。**
    **選択肢の文字は押せない**（消すことと選ぶことを同じ画面に混ぜない）。正しいものを
    1つ選びたければ「4択に切り替える」で降りる。計算問題には割り当てない（`hardMode: "choice"`）
  - **文字パネル** — 3×3／4×4 をなぞって読みを作る。**文字数の枠も1文字目のマークも
    出さない。** 盤面は問題IDから決まるので、同じ問題なら同じ盤面が出る。ダミーには
    答えの文字を1〜2枚まぜて偽の分岐を作る。連続タップでも成立する。読みが13文字以上、
    または経路を100回で引けなければ消去法へ落ちる。
    **マスは四角ではなく、内側を向いた角だけを落とした形です。** 中は八角形、辺は
    六角形、四隅は五角形になり、切った辺どうしが向き合います。`clip-path` は当たり判定も
    削るので、ななめにたどるときに隣のマスの角をかすめて拾うことがなくなります。
    形に沿った縁は、外側を縁の色で塗り、1px内側にもう一度同じ形を切り抜いて作ります
    （枠線と inset の影は `clip-path` に切られて途切れます）。
    隣り合うマスは薄い線で結んであり、**ななめにもたどれる**ことが触る前に分かります。
    指の位置は座標で拾い、マスの中心から半径0.46までで反応します
  - **スワイプ** — 中央に写真、上に短い問い、左右に2つの答え。
    **カードを正しいと思うほうへはらう。** 押しても矢印キーでも同じで、
    踏み切る前（56px未満）で放せば戻る。**途中に解説もヒントも挟まず**、
    ○✕ だけをその場で出して0.9秒で次へ行く。**解説はセッションの終わりの
    「ふりかえり」にまとめて出る**（外した問題は開いた状態・原則3）。
    報酬はほかの方式と1つも変わらない（原則3-2）。ふつうのセッションには
    **8問の束**として出る。**スワイプだけの10問**という入口も「出題を選ぶ」画面にある。
    現在16問（解放前の範囲に10問）
  - **数値入力** — テンキーで数だけを打ち、**単位は固定表示**（`[ 200 ] W`）。
    打ち間違いを判定の対象にしないので、判定は完全に機械的。**外しても問題は終わらない。**
    入力欄は生きたままで、続ける・ヒントを見る・4択に降りるの3択になる。
    「このまま挑み直す」に専用ボタンは置かない（押しても何も起きないボタンは
    選択肢の重みを下げる）
- 知識マップ、知識カード、クリスタルとクラフト、装備
- **チャレンジバトル**（3問構成・難易度ゲージ・自由入力・表記ゆれ吸収）
- **出題形式ごとの段（Lv.1〜Lv.3）** — 学年とは別の軸で、同じ学年の中の踏みこみ方。
  問題ごとに `level` を持つ（834問すべてに付与済み）。**形式ごとに別**で、その形式の束で
  4分の3以上とれた回に1つ上がる。**上がるだけで下がらない**（原則3）。報酬は変わらない
  （原則3-2）。学年チップの隣に `Lv.2` を出し、上がった回はリザルトで知らせる
- **1セッションは出題形式の束3つ**（`engine.planRun`）— 形式はくじで3つ選び、
  形式ごとにまとめて出す。束の長さは操作の手数なりで、スワイプ8問・文字パネル3問。
  合計は9〜18問（中央値14）で毎回変わる。ヘッダの帯に3つの束と進み具合が出る。
  **形式の選び方に正答率や履歴は使わない**（得意不得意で偏らせないため）
- **出題の画面は4つの層**（S-05〜S-10 / S-26）— バトル演出・設問・補足・解答。
  **ヒントと設定はポップアップ**（S-27 / S-28）で、元の画面を動かさない。
  **束の切れ目には接敵（S-29）と戦果（S-30）**を挟む。英雄が画面の外から入り、
  敵の体力が満ちて GO!。束が終わると撃破！／逃げられた……と、その束ぶんの
  成績と報酬が出る。**束があるときだけ**挟む（名指しの1問には挟まない）。
  **1束が1 wave**で、正解すると敵を、外すとこちらが殴られる。敵の体力は
  「その束の8割に正解したら倒れる」ところに置き、残りの問題数から決め直す。
  **英雄が倒れても wave は続き、1問ごとの報酬は1つも変わらない**（原則3）。
  倒した敵と倒しきった束のぶんだけ GUM と点が上乗せされる
- **必殺技**（S-32 / S-33）— **解説を省く設定でも応用編に挑める2つ目の入口。**
  隙を見せるかは問題IDと英雄から決まり、サイコロは振らない。応用編を抜けると
  MCH のパッシブスキル演出のカットインが出て、敵に3倍の一撃。
  **動くのは敵の体力だけ**（GUM も知識カードも増えない）
- 出題の重複解消、解説スキップ設定
- **UI基盤** — ホームだけ 100dvh の3層。上段は内容ぶん（実測22〜27%）、
  下段32%（縦の狭い端末は29%）、主役層が残りを吸収する。背景はホーム全体に敷く。
  ホーム以外は方眼紙のまま
- ステータス層 — ユーザーアイコン・称号・ユーザー名・残バッテリー・時刻と、
  **知識マップの進み具合**（時計と同じ行。押すと知識マップ画面が開く）、
  マイちゃんの吹き出し。吹き出しは文字数ぶん伸び、3行を超えたら9pxまで字を縮める
- 挑む相手はボタンの外に大きく出し、未解放の英雄を4秒ごとに自動で送るカルーセル
  （矢印は置かない。指で払っても動く。動きを減らす設定では自動送りを止める）。
  **どれだけゲージを削れるかはホームに出さない。** 挑戦先を選ぶ画面で見せる
- 手持ち英雄・知識カード・クリスタル・図鑑・知識マップはヒーロー画面に集約（2タブ）。
  **知識マップはホームからも1タップで開きます**（`vMap`）。図鑑の英雄カードは、
  解放済みでも到達度を出します
- **GUM** — 正解1問につき1〜10（学年なり）。**所持数はショップ画面だけに出します。**
  リザルトと日別の記録には「今回いくつ貯まったか」だけを出し、所持は出しません。
  ホームの一等地は知識マップに渡しました（原則7・`docs/research-edtech.md` §2）
- **称号とマイページ** — ステータス層を押すとマイページ。名前（255文字まで）・
  アイコン（手持ちの英雄）・称号を差し替えられる。未取得の称号は名前を伏せ、
  条件と進み具合だけ見せる。**「量」より「越境」を先に並べる**
- **カレンダー** — 解いた日にスタンプ。日を選ぶと成績と各問の解説を読み返せる。
  そこから再挑戦もできるが、クリスタルも知識カードも点も入らず、記録も変わらない
- **エクステンション108種**（由来のある品だけ）— 独自ランクは**由来がいくつの教科に
  またがるか**で決まる（初伝61・中伝43・奥伝4）。MCHのレアリティは使わない。
  レシピは族ごとのポイント＋**またがる教科それぞれの知識カード**。素材だけでは作れない。
  クラフト画面はランクごとのタブで、作れるものから順に並ぶ。説明文はその品の由来そのもの
- **クリスタル100種とショップ**（001-100）— GUMで買う。品揃えは固定で安い順。価格は
  希少度から自動算出で、どれを選んでも GUM あたりの重みは同じ。**族は6つで、教科と1対1**。
  豆知識は手に入れると読める。**クリスタルは知識カードにはしない**（下記）。
  称号「石を読む者」は5種集めると開く
- **クリスタルは確率で落ちる**（原則2の唯一の例外）— 正解すると、その教科の族で1回抽選する。
  族の中は図鑑の希少度そのままの重み。**出にくい族ほど1回の当たりが大きく、どの族でも
  規定ポイントに届くまでの時間は変わらない。** レア寄りだけ最大10%の上乗せ。
  外れても何も失わず、画面にも「出なかった」とは書かない。
  手に入れた鉱物は**図鑑に残り、族ポイントに変わる**（クラフトでは減らない）
- **由来カードと、それが開く問い** — 奥伝4種は、その品の元になった人物や出来事の問題
  （玄奘三蔵・トロイアの木馬・エラトステネス・経度と時計）を解かないと作れない。
  クラフト画面から直接たずねられる。**その4枚はさらに8問を開く。** 報酬のゴールを
  「所持数」ではなく「次の問い」にするため（原則7）
- **クラフトは族ごとのポイント＋知識カード** — 「貴金属140pt・社会の札12枚」のように要求する。
  個別の鉱物は名指ししない。要求する族は、その品の由来がまたがる教科の族
  （`subs` → `subjectToFamily`）。釣り合いは `data.js` の `buildExtensions` が導くので、
  台帳には数字を書かない
- **チャレンジ再設計** — 1回3問。必要正答数はレアリティなり（Common 1、Rare 2、
  Legendary 3）。**どの段階の問い方も、その人物を知らないと答えられない。**
  ゲージが変えるのは問いの難しさだけ。挑むたびにその人物の知識カードを1枚持ち帰る
- **難易度ゲージは「到達度」** — 直結カードの網羅率×0.6＋関連分野の網羅率×0.4を、
  装備倍率（最大2倍）で伸ばし、レアリティの難度係数で割る。HPと `gearCap` は撤去した。
  **装備画面で、誰への到達度を見るかを選べる。** 装備の候補それぞれに差分が並び、
  選んだ瞬間に `46% → 58%` と動く。**初伝ひとつの効きは1%に満たないことがあるので、
  整数で同じに見えるときだけ小数第1位まで出す**（丸めると「動いた」が画面から消える）
- **レンジ回答** — 年を幅で答える。許容幅は正解年から自動算出（`engine.rangeWidth`）。
  狭いほど高得点で、精度800点以上なら抽選がもう1回。**答える前に点数の見込みは出さない**
- 進行の localStorage 保存（`sekai-no-kyoushitsu/v1`）。日別の記録は `days`

## 次にやること

`docs/implementation-plan.md` に詳細があります。順番は次のとおり。

1. ~~**UI基盤**~~ — 実装済み。ショップのボタンは枠だけ置いて `disabled`（4番で中身を入れる）
2. ~~**レンジ回答**~~ — 実装済み。社会に10問入れてある。他教科への展開が残り
3. ~~**チャレンジ再設計**~~ — 実装済み（9体すべて3問構成に書き直した）
4. ~~**ショップ・クリスタル・40種エクステンション**~~ — 実装済み
5. ~~**称号・カレンダー**~~ — 実装済み（「石を読む者」も追加済み）
6. **難モード（選択と裁量）** — `docs/experience-design-framework.md` の決定2・決定4
   a. ~~消去法~~  … 実装済み。209問。計算問題は `hardMode: "choice"` で外してある
   b. ~~数値入力~~ … 実装済み。53問。テンキー、単位は固定表示、外しても終わらない
   c. ~~文字パネル~~ … 実装済み。185問。`reading` を足すほど消去法から移る（残り57問）。
      残っているのは述語（走る・減る）や説明句が答えの問題で、**そこには付けません**
   d. ~~無説明の初回起動~~ … 実装済み。小1の5問から始まり、3問目で難モードが出る
   e. ~~スワイプ（2択・テンポ優先）~~ … 実装済み。16問。**写真が要るので、
      ここから先の伸びは写真の枚数で決まります。** いまは台帳にある16枚を使い切った
      ところ。`data/images.json` に `search` と `alt` を書いて
      `node scripts/fetch-commons.mjs`（手動・要ネットワーク）を走らせると増やせます
7. **報酬系の再設計** — `docs/reward-economy.md`
   a. ~~ゲージを網羅率へ差し替え~~ … 実装済み。HP と `gearCap` は撤去した
   b. ~~装備画面の before/after 表示~~ … 実装済み。誰への到達度を見るかを選べる
   c. ~~魔石をクリスタルへ一本化~~ … 実装済み。魔石は跡形なく消した
   d. ~~`extensions-curated.json` の導入とクラフト条件への知識カード追加~~ … 実装済み。
      40種の8系統×5レアリティは引退し、由来のある108種に置き換えた
   e. ~~由来カードと、それが解放する問題~~ … 実装済み。奥伝4種に由来カードを付け、
      その問い4問と、そこから開く8問を書いた
   f. ~~GUM をショップ画面へ、知識マップをホームへ~~ … 実装済み

**c は完了しました。** クリスタルを100種に増やして6族を埋め、クラフトの要求を族ごとの
ポイントにし、正解するとその教科の族で抽選が回り、魔石は跡形なく消えました。
通貨はクリスタル（族ポイント）と GUM の2つだけです。
8. **問題DB拡張** — 全期間ずっと並行。**ここが最終的にゲームの寿命を決めます**
   - 第1段（48マス×10問＝480問）は達成。次の段は 1マス30問（1440問）

この順番の外に、将来構想として**投資・貯蓄シミュレータ**（GUM を公民・経済の実践として
運用して増やす）を `docs/implementation-plan.md` §8 に置いてあります。**当面のスコープ外です。**
原則2（ランダム報酬を入れない）と正面からぶつかる論点があるので、着手する前に §8 の表を
読んでください。

---

## 判断に迷ったら

`docs/design-decisions.md` に、これまでに検討して**却下した案とその理由**をまとめてあります。「なぜこうなっているのか」が分からない箇所は、たいていそこに書いてあります。

ゲームサイクルまわりは `docs/experience-design-framework.md`（体験デザインの枠組みと、決定1〜4の確定仕様）も併せて読んでください。

報酬系は `docs/reward-economy.md`、その根拠となる調査は `docs/research-edtech.md`
（図表つきの読みやすい版が `docs/research-edtech.html`）にあります。

**いまの実装が三層のどこで切れているかは `docs/experience-tsui.html` です。**
直感（仮説→試行→歓喜）・驚き（誤解→試行→驚愕）・物語（翻弄→成長→意志）の3つの輪に
現行の画面と仕組みを置き直して、回っている拍と切れている拍を色分けしてあります。
**いま切れているのは3か所** — 束の1問目に仮説が立たないこと、「知っているつもり」を
作る装置が無いこと、そして**意志を置く場所が決まっていないこと**。
`docs/research-edtech.md` が調べた5サービスも、最後の1拍だけは実装できていませんでした。

**`docs/review-points.md` は、機械では判定できないものの置き場です。** 画面の見え方、
由来の文章が断定しすぎていないか、確認を取らずに決めた判断、数字の釣り合い。
**勝手に決めたことは、ここに書き出してから進めてください。** 黙って決めると、
あとから追えなくなります。

書いていない論点にぶつかったら、勝手に決めずに確認を取ってください。特に報酬設計と難易度設計は、コンセプトに直結するので慎重に扱ってください。
